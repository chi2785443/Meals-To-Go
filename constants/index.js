import images from "./images";
import icons from "./icons";
import { fonts, fontWeights, fontSizes } from "./fonts";
import { COLORS, SIZES, SHADOWS } from "./theme";

export { images, icons, COLORS, SIZES, SHADOWS, fonts, fontWeights, fontSizes };
